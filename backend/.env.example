# All values are optional. With no key set, the app runs fully on the
# deterministic template engine — this is the supported evaluation mode.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5
DB_PATH=audit.db
